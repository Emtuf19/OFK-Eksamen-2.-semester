<Page
    x:Class="_2_Semester_Eksamen.Views.HomeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:_2_Semester_Eksamen.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="HomeWindow"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    Background="White"
    mc:Ignorable="d">
    
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label
                Grid.Row="0"
                Grid.ColumnSpan="4"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                Odense Fægte-Klub
            </Label>
            
            <!--  List over Træning  -->
            <Border
                Grid.Row="1"
                Grid.Column="1"
                Margin="0,20,20,30"
                BorderBrush="Black"
                BorderThickness="2">
                <Grid Background="#FFF3F35D">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="25"
                        FontWeight="Bold"
                        Text="Træninger:" />
                    <TextBlock
                        x:Name="TrainingError_txt"
                        Visibility="Hidden"
                        Grid.Row="0"
                        Margin="40,40,0,0"                       
                        FontWeight="Bold"
                        FontSize="16"
                        Text="{Binding Error}"/>
                    <ListBox
                        FontSize="16"
                        Grid.Row="1"
                        Background="Transparent"
                        BorderThickness="0"
                        ItemsSource="{Binding Practices}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="5">
                                    <TextBlock FontWeight="Bold" Text="{Binding PracticeName}" />
                                    <ItemsControl ItemsSource="{Binding Trainers}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Rectangle Height="1" Fill="Black"></Rectangle>
                                                    <TextBlock FontWeight="Bold" Text="Træner: " />
                                                    <TextBlock Text="{Binding TrainerFirstName}" />
                                                    <TextBlock Text="{Binding TrainerLastName}" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontWeight="Bold" Text="Start: " />
                                        <TextBlock Text="{Binding StartTime, StringFormat=dd-MM-yyyy HH:mm}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontWeight="Bold" Text="Slut: " />
                                        <TextBlock Text="{Binding EndTime, StringFormat=dd-MM-yyyy HH:mm}" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!--  Preview over Kalender  -->
            <Border
     Grid.Row="1"
     Grid.Column="2"
     Margin="0,20,20,30"
     BorderBrush="Black"
     BorderThickness="2">
                <Grid Background="#FFF3F35D">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
             HorizontalAlignment="Center"
             FontSize="25"
             FontWeight="Bold"
             Text="Begivenheder:" />
                    <TextBlock
             Grid.Row="0"
             FontSize="16"
             Margin="40,40"                       
             FontWeight="Bold"                        
             Text="{Binding ErrorEvent}" />
                    <ListBox Background="Transparent" BorderThickness="0">                        
                    </ListBox>
                </Grid>
            </Border>            
        </Grid>
    </Grid>
</Page>
